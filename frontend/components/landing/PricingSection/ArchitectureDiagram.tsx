export function ArchitectureDiagram() {
    return (
        <svg
            className="absolute inset-0 h-full w-full p-4 md:p-8"
            viewBox="0 0 1000 400"
            preserveAspectRatio="xMidYMid meet"
        >
            {/* We'll copy the SVG exactly from the HTML */}
        </svg>
    );
}